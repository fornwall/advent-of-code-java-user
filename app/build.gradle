plugins {
    id 'application'
}

repositories {
    mavenCentral()
}

dependencies {
    implementation 'net.fornwall:aoc:+'
    testImplementation 'org.junit.jupiter:junit-jupiter:+'
}

application {
    mainClass = 'advent.of.code.java.user.App'
}

tasks.named('test') {
    useJUnitPlatform()
}
